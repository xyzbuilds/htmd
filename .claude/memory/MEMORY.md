# Memory index

- [Project vision: htmd as a bidirectional MD↔HTML bridge](project_htmd_vision.md) — htmd's purpose is the *loop*, not the artifact: agents emit MD with embedded widget fences, humans interact, one button copies aggregated changes back as a prompt.
- [Compose architecture and export protocol](project_compose_architecture.md) — `htmd compose` parses ```htmd:<template>``` fences; templates register into `window.__htmd.blocks` so the global FAB can aggregate.
- [User's working preferences](user_working_preferences.md) — non-supervised mode, iterate aggressively, no clarifying questions when told "just go".
