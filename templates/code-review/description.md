# code-review

Annotated code blocks (one or more files; supports add/del diff hunks). Agent can pre-place comments; human clicks any line number to add inline comments. The export button copies all human comments back to the agent as a structured prompt.
