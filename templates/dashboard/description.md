# dashboard

KPI grid: big-number cards with optional sparklines, deltas, and targets.

**When to use:** Executive metrics views, weekly KPI snapshots, OKR tracking, monitoring dashboards.

**Token budget:** ~120 tokens of YAML produces ~3,000-token rich HTML with inline SVG sparklines.
