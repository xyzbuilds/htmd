# data-table

Sortable, filterable table from `columns` + `rows`. Per-column type formatting (number, currency, percent, date, badge, mono). Filter + sort state can be exported back as a markdown table.
