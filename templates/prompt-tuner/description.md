# prompt-tuner

Editable prompt template + N sample variable sets with live previews and copy-to-clipboard.

**When to use:** Iterating on prompts with stakeholders, prompt review docs, exploring template variations before locking into code.
