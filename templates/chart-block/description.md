# chart-block

Single inline-SVG chart with title, optional caption, and (opt-in) underlying-data table. Wraps the existing `chart` helpers (sparkline / bar / line / donut) for use as a standalone block in compose pages.
